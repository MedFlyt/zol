language: node_js
branches:
    only:
        - master
        - /^greenkeeper/.*$/
cache:
    yarn: true
    directories:
        - node_modules
notifications:
    email: false
node_js:
    - '6'
before_script:
    - ./node_modules/.bin/lerna bootstrap
script:
    - ./node_modules/.bin/lerna run test:prod && lerner run build
after_success:
    - ./node_modules/.bin/lerna run report-coverage
